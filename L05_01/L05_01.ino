// Korzystając z programu zamieszczonego jako kod 3, proszę zaobserwować w zestawie 
// laboratoryjnym jak zachowują się wyprowadzenia enkodera przy obrocie w lewo i przy 
// obrocie w prawo o jedną pozycję. Odnotować, a wręcz narysować sobie, sekwencję 
// stanów przy obrocie do kolejnej pozycji spoczynkowej (o jeden „ząbek”), zarówno w 
// lewo, jak i w prawo. Proszę odpowiedzieć jak, patrząc tylko na przebieg stanów 
// wyprowadzeń w czasie, można stwierdzić, w którą stronę obrócono
// enkoder i o ile pozycji. Proszę opisać krótko słownie algorytm, który to realizuje.

// Sformułujmy teraz problem programistyczny, który enkoder ma rozwiązać. Program ma zmieniać, 
// przy obrocie enkoderem, łagodnie kolor diody RGB z niebieskiej na czerwoną przy obrocie 
// enkodera w prawo, i przeciwnie przy obrocie w lewo. Wartość zmiennej sterującej, ustawianej 
// przez enkoder, ma być wyświetlana na wyświetlaczu LCD. Należy wyeliminować zjawisko
// drgania styków, które występuje tak samo jak w przypadku przycisków. Można zrealizować wiele 
// algorytmów odczytu pozycji enkodera. W kodzie 4 zawarta jest propozycja odczytywania. Opis
// programu znajduje się pod kodem.

void setup() {
}


void loop() {
}
